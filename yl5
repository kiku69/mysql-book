SELECT YEAR(o.order_date) AS Aasta, COUNT(o.id) AS "Tellimuste arv", ROUND(COALESCE(SUM(b.price), 0), 2) AS "Müükide summa" FROM d124553_bookstore.orders o LEFT JOIN d124553_bookstore.books b ON o.book_id = b.id GROUP BY  YEAR(o.order_date)
